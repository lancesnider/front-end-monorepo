import { shallow } from 'enzyme'
import React from 'react'

import MenuLinksNarrow from './MenuLinksNarrow'

let wrapper

describe('Component > MenuLinksNarrow', function () {
  before(function () {
    wrapper = shallow(<MenuLinksNarrow />)
  })

  it('should render without crashing', function () {})
})
