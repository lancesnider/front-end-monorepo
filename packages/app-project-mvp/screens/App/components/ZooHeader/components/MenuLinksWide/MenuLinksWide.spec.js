import { shallow } from 'enzyme'
import React from 'react'

import MenuLinksWide from './MenuLinksWide'

let wrapper

describe('Component > MenuLinksWide', function () {
  before(function () {
    wrapper = shallow(<MenuLinksWide />)
  })

  it('should render without crashing', function () {})
})
